{
  "name": "{{corePackageName}}",
  "version": "1.0.0",
  "description": "Core (domain + orchestration) for feature {{pascalCase featureName}}",
  "private": true,
  "type": "module",
  "exports": {
    "./models": "./domain/models/index.ts",
    "./operations": "./domain/operations/index.ts",
    "./use-cases": "./orchestration/use-cases/index.ts",
    "./ports": "./orchestration/ports/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run"
  },
  "dependencies": {
    "@xndrjs/branded": "{{workspaceDependencyVersion "@xndrjs/branded"}}",
    "@xndrjs/orchestration": "{{workspaceDependencyVersion "@xndrjs/orchestration"}}",
    "zod": "{{workspaceDependencyVersion "zod"}}"
  },
  "devDependencies": {
    "vitest": "{{workspaceDependencyVersion "vitest"}}"
  }
}
