{
  "name": "{{corePackageName}}",
  "version": "1.0.0",
  "description": "Core for feature {{pascalCase featureName}}: domain models, operations, use cases, and ports",
  "private": true,
  "type": "module",
  "exports": {
    "./models": "./domain/models/index.ts",
    "./operations": "./domain/operations/index.ts",
    "./use-cases": "./orchestration/use-cases/index.ts",
    "./ports": "./orchestration/ports/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run"
  },
  "dependencies": {
    "@xndrjs/domain-zod": "{{workspaceDependencyVersion "@xndrjs/domain-zod"}}",
    "zod": "{{workspaceDependencyVersion "zod"}}"
  },
  "devDependencies": {
    "vitest": "{{workspaceDependencyVersion "vitest"}}"
  }
}
