import { z } from "zod";
import { domain, zodToValidator } from "@xndrjs/domain-zod";

// TODO: tighten schema (bounds, formats, …) for your domain
const {{pascalName}}Schema = {{{primitiveSchemaExpr}}};

export const {{pascalName}} = domain.primitive(
  "{{pascalName}}",
  zodToValidator({{pascalName}}Schema)
);

export type {{pascalName}} = ReturnType<typeof {{pascalName}}["create"]>;
