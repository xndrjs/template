import { z } from "zod";
import { domain, zodToValidator } from "@xndrjs/domain-zod";

export const {{pascalName}}Schema = z.object({
  // Type discriminant for this shape. Remove it at your risk.
  kind: z.literal("{{pascalName}}").default("{{pascalName}}"),
  // TODO add shape properties
  // payload: z.string().min(1),
});

export const {{pascalName}}Shape = domain.shape(
  "{{pascalName}}",
  zodToValidator({{pascalName}}Schema)
);

export type {{pascalName}} = ReturnType<typeof {{pascalName}}Shape["create"]>;
