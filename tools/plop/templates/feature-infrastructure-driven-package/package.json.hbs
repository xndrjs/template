{
  "name": "@features/{{featureKebab}}-infra-driven-{{drivenKebab}}",
  "version": "1.0.0",
  "description": "Driven outbound adapters (driven-{{drivenKebab}}): flat TypeScript modules at package root, re-exported from index.ts.",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run"
  },
  "dependencies": {
    "@features/{{featureKebab}}-core": "workspace:*"{{#if hasOptionalXndrDeps}},
{{/if}}
{{#if includeDataLayer}}
    "@xndrjs/data-layer": "{{workspaceDependencyVersion "@xndrjs/data-layer"}}"{{#if includeTasks}},
{{/if}}
{{/if}}
{{#if includeTasks}}
    "@xndrjs/tasks": "{{workspaceDependencyVersion "@xndrjs/tasks"}}"
{{/if}}
  },
  "devDependencies": {
    "vitest": "{{workspaceDependencyVersion "vitest"}}"
  }
}
